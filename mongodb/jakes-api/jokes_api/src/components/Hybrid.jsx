import React from 'react'
import Axios from 'axios'

const Hybrid = (props) => {

    const onSubmitHandler = (e) => {
        // using uri from props, we can determine if we are updating or submitting a new one.
        // if(props.uri.includes('update')) {
        //     Axios.put('http://localhost:8000/api/v1/edit/:id')
        // } else {
        //     Axios.post('lsdkfjdlkfj')
        // }
    }
    return (
        <div>
            
        </div>
    )
}

export default Hybrid
